# SpeakCoach — Python AI 服务
# FastAPI 网关 + LangGraph workflow + GPT-SoVITS TTS 客户端。
# 本文件是规范的依赖列表。pyproject.toml 同步反映这份内容。
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# LangGraph / LangChain workflow
langgraph>=0.2.0
langgraph-checkpoint>=2.0.0
langgraph-checkpoint-redis>=0.4.0,<0.5.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.0
langchain-anthropic>=0.1.0
langchain-community>=0.2.0

# HTTP / IO
httpx>=0.27.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# Speech analysis
whisperx>=3.3.0

# 持久化（供 langgraph-checkpoint-redis 使用）
redis>=5.0.0

# 测试依赖（这样仅 `pip install -r requirements.txt` 就足够跑 `pytest`）
pytest>=8.0.0
pytest-asyncio>=0.23.0
